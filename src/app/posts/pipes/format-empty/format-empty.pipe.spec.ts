import { FormatEmptyPipe } from './format-empty.pipe';

describe('FormatEmptyPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatEmptyPipe();
    expect(pipe).toBeTruthy();
  });
});
